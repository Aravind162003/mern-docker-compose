DEPLOY MERN-STACK APPLICATION

FRONTEND
- cd frontend
- npm install
- write Dockerfile (check frontend folder)

BACKEND
- cd backend
- npm install
- write docker file (check backend folder)

WRITE docker-compose.yaml file (check in github repository)

USE SINGLE COMMAND TO CREATE AND RUN THE CONTAINER:

- docker compose up -d
